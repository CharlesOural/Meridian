# Included automatically by downstream find_package(meridian_cmake). Puts the shared
# modules on the module path so a consumer can include(MeridianToolchain) etc.
list(APPEND CMAKE_MODULE_PATH "${meridian_cmake_DIR}")
